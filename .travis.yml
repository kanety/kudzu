sudo: false
language: ruby
rvm:
  - 2.3.5
  - 2.4.2
before_install:
  - gem install bundler -v 1.15.3
before_script:
  cd spec/dummy
  bin/rails server -p 9292 -b localhost -d -e test
  cd ../..
script: bundle exec rspec
